server:
  port: 8080

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

zuul:
  routes:
    customer-service: /customer/**
    admin-service: /admin/**
    order-service: /order/**
